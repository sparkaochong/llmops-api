[pytest]
# 缓存目录（默认散落在各测试目录下）
cache_dir = tmp/.pytest_cache

# 默认命令行选项（无需每次手动输 -v -s）
addopts = -v -s

# 测试文件发现规则（以下是 PyTest 默认值，可省略）
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*